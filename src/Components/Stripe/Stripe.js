import React from "react";
import "./Stripe.css";

const Stripe = () => {
  return (
    <div className="tooki__stripe-container_stripe">
      <div className="tooki__stripe-container_stripe-content">
        <h1>400%</h1>
        <h1>/</h1>
        <div className="tooki__stripe-container_stripe-content-div">
          <p>
            If quality social proof buffers notable uncertainty, get ready for
            some remarkable conversion impact — in some cases up to 400%
            improvement
          </p>
          <h5>CONVERSIONXL</h5>
        </div>
      </div>
    </div>
  );
};

export default Stripe;
